import { IonTitle } from "@ionic/react";
import styled from "styled-components";

const SosmedQuTitle = styled(IonTitle)`
    font-family: inter;
    font-weight: bold;
    font-size: 24px;
    padding: 16px;
`;

export { SosmedQuTitle };