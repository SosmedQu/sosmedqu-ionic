import { IonLoading } from "@ionic/react";
import styled from "styled-components";

const Loading = styled(IonLoading)`
    // --backdrop-opacity: 0.7;
    // --background: var(--ion-color-light);
    ion-backdrop {
        // background: var(--ion-color-light);
    }
`;

export { Loading };